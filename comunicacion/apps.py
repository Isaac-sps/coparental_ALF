from django.apps import AppConfig


class ComunicacionConfig(AppConfig):
    """Configuración de la app comunicación."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "comunicacion"

